$maxSteps := 0
for $start in 1..100000 {
  $x := $start
  $steps := 0
  while $x > 1 {
    if $x % 2 == 0 { $x = int($x / 2) }
    else { $x = 3 * $x + 1 }
    $steps = $steps + 1
  }
  if $steps > $maxSteps { $maxSteps = $steps }
}
say($maxSteps)
