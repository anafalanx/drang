module github.com/anafalanx/drang

go 1.26

// drang depends only on the Go standard library plus golang.org/x/sys/windows (the Go
// team's Win32 binding, for the console APIs and Job Objects).

require golang.org/x/sys v0.46.0
